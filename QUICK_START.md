# ⚡ Быстрый старт для преподавателя

Этот документ поможет быстро развернуть и проверить систему.

## 🎯 Вариант 1: Проверка через публичный деплой (Рекомендуется)

Если система уже развернута на Render.com или Railway.app:

1. Откройте URL frontend (например: `https://classification-frontend.onrender.com`)
2. Войдите с учетными данными:
   - **Username**: `admin`
   - **Password**: `admin123` (или тот, что был установлен)
3. Протестируйте функционал:
   - Классификация одного товара
   - Загрузка CSV файла
   - Управление пользователями (для админа)

## 🎯 Вариант 2: Локальный запуск через Docker

### Требования:
- Docker и Docker Compose установлены
- Git

### Шаги:

1. **Клонируйте репозиторий**
   ```bash
   git clone <repository-url>
   cd classification-system
   ```

2. **Запустите все сервисы**
   ```bash
   docker-compose up --build
   ```

   Это запустит:
   - PostgreSQL базу данных
   - Backend API (порт 5000)
   - Frontend (порт 80)

3. **Инициализируйте базу данных**
   
   В новом терминале:
   ```bash
   docker exec -it classification_backend python src/init_db.py
   ```

4. **Откройте в браузере**
   - Frontend: http://localhost
   - Backend API: http://localhost:5000/health

5. **Войдите в систему**
   - Username: `admin`
   - Password: `admin123`

## 🎯 Вариант 3: Ручной запуск (для разработки)

### Backend:

```bash
cd backend
pip install -r requirements.txt

# Настройте .env файл или переменные окружения
export DATABASE_URL="postgresql://postgres:1234@localhost:5432/product_classifier"
export SECRET_KEY="your-secret-key"
export JWT_SECRET_KEY="your-jwt-secret"

# Инициализируйте БД
python src/init_db.py

# Запустите сервер
python src/main.py
```

### Frontend:

```bash
cd frontend
npm install

# Настройте API URL (опционально)
export VITE_API_URL="http://localhost:5000/api"

# Запустите dev сервер
npm run dev
```

## 📋 Чек-лист для проверки

- [ ] Система запускается без ошибок
- [ ] Можно войти с учетными данными админа
- [ ] Классификация одного товара работает
- [ ] Загрузка CSV файла работает
- [ ] Результаты отображаются корректно
- [ ] Админ может создавать/удалять пользователей
- [ ] Health check endpoint возвращает `{"status": "ok"}`

## 🔍 Тестовые данные

### Пример товара для классификации:
```
Смартфон Apple iPhone 14 Pro Max 256GB
```

### Пример CSV файла:
```csv
product_name
Смартфон Apple iPhone 14 Pro Max 256GB
Ноутбук ASUS ROG Strix G15
Наушники Sony WH-1000XM4
```

## 🐛 Решение проблем

### Backend не запускается
- Проверьте, что PostgreSQL запущен
- Проверьте переменные окружения (особенно DATABASE_URL)
- Посмотрите логи: `docker-compose logs backend`

### Frontend не подключается к API
- Проверьте, что backend запущен на порту 5000
- Проверьте переменную `VITE_API_URL`
- Проверьте CORS настройки в backend

### Ошибки базы данных
- Убедитесь, что БД инициализирована: `python src/init_db.py`
- Проверьте подключение к БД
- Проверьте, что таблицы созданы

## 📞 Контакты

Если возникли проблемы при проверке, свяжитесь с автором проекта.

---

**Успешной проверки! 🎉**

