# 🚀 Инструкция по деплою системы классификации товаров

Этот документ описывает процесс развертывания системы на публичных платформах.

## 📋 Предварительные требования

1. GitHub аккаунт с репозиторием проекта
2. Аккаунт на одной из платформ:
   - [Render.com](https://render.com) (рекомендуется - бесплатный план)
   - [Railway.app](https://railway.app) (бесплатный план с ограничениями)
   - [Fly.io](https://fly.io) (бесплатный план)

## 🎯 Вариант 1: Деплой на Render.com (Рекомендуется)

### Шаг 1: Подготовка репозитория

1. Убедитесь, что все файлы закоммичены и запушены в GitHub:
   ```bash
   git add .
   git commit -m "Add CI/CD and deployment configuration"
   git push origin main
   ```

### Шаг 2: Создание аккаунта на Render

1. Перейдите на [render.com](https://render.com)
2. Зарегистрируйтесь через GitHub
3. Подтвердите email

### Шаг 3: Создание базы данных PostgreSQL

1. В Dashboard нажмите "New +" → "PostgreSQL"
2. Настройки:
   - **Name**: `classification-db`
   - **Database**: `product_classifier`
   - **User**: `postgres`
   - **Plan**: Free
3. Нажмите "Create Database"
4. Скопируйте **Internal Database URL** (понадобится позже)

### Шаг 4: Деплой Backend

1. В Dashboard нажмите "New +" → "Web Service"
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name**: `classification-backend`
   - **Environment**: `Docker`
   - **Dockerfile Path**: `./backend/Dockerfile`
   - **Docker Context**: `./backend`
   - **Plan**: Free
4. Добавьте Environment Variables:
   ```
   FLASK_ENV=production
   FLASK_DEBUG=False
   DATABASE_URL=<вставьте Internal Database URL из шага 3>
   SECRET_KEY=<сгенерируйте случайную строку>
   JWT_SECRET_KEY=<сгенерируйте случайную строку>
   CORS_ORIGINS=https://classification-frontend.onrender.com,http://localhost:5173
   ```
5. Нажмите "Create Web Service"
6. Дождитесь завершения деплоя (5-10 минут)
7. Скопируйте URL вашего сервиса (например: `https://classification-backend.onrender.com`)

### Шаг 5: Деплой Frontend

1. В Dashboard нажмите "New +" → "Web Service"
2. Выберите тот же репозиторий
3. Настройки:
   - **Name**: `classification-frontend`
   - **Environment**: `Docker`
   - **Dockerfile Path**: `./frontend/Dockerfile`
   - **Docker Context**: `./frontend`
   - **Plan**: Free
4. Добавьте Environment Variables:
   ```
   VITE_API_URL=https://classification-backend.onrender.com/api
   ```
   (Замените на ваш URL backend)
5. Нажмите "Create Web Service"
6. Дождитесь завершения деплоя

### Шаг 6: Инициализация базы данных

После деплоя backend нужно создать таблицы в БД:

1. Откройте терминал в Render Dashboard для вашего backend сервиса
2. Выполните:
   ```bash
   python -c "from database.models import db; from api.app import create_app; app = create_app(); app.app_context().push(); db.create_all()"
   ```

Или создайте скрипт `backend/src/init_db.py`:
```python
from api.app import create_app
from database.models import db, User

app = create_app()
with app.app_context():
    db.create_all()
    # Создайте первого админа
    if not User.query.filter_by(username='admin').first():
        admin = User(username='admin', role='admin')
        admin.set_password('admin123')  # Измените пароль!
        db.session.add(admin)
        db.session.commit()
        print("✅ База данных инициализирована!")
```

Затем выполните в терминале Render:
```bash
python src/init_db.py
```

## 🎯 Вариант 2: Деплой на Railway.app

### Шаг 1: Подготовка

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Подключите GitHub аккаунт

### Шаг 2: Создание проекта

1. Нажмите "New Project" → "Deploy from GitHub repo"
2. Выберите ваш репозиторий

### Шаг 3: Добавление PostgreSQL

1. В проекте нажмите "+ New" → "Database" → "PostgreSQL"
2. Railway автоматически создаст БД и добавит переменную `DATABASE_URL`

### Шаг 4: Деплой Backend

1. Нажмите "+ New" → "GitHub Repo"
2. Выберите ваш репозиторий
3. Railway автоматически определит Dockerfile
4. Добавьте переменные окружения:
   - `FLASK_ENV=production`
   - `FLASK_DEBUG=False`
   - `SECRET_KEY=<случайная строка>`
   - `JWT_SECRET_KEY=<случайная строка>`
5. Railway автоматически задеплоит сервис

### Шаг 5: Деплой Frontend

1. Добавьте еще один сервис из того же репозитория
2. Укажите root directory: `frontend`
3. Добавьте переменную: `VITE_API_URL=<ваш-backend-url>/api`

## 🔧 Локальный запуск с Docker

Для тестирования перед деплоем:

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd classification-system

# Создайте .env файл (опционально)
cp .env.example .env

# Запустите все сервисы
docker-compose up --build

# Backend будет доступен на http://localhost:5000
# Frontend будет доступен на http://localhost:80
# PostgreSQL на localhost:5432
```

## 🔍 Проверка работоспособности

После деплоя проверьте:

1. **Health check**: `https://your-backend-url/health`
   - Должен вернуть: `{"status": "ok"}`

2. **Frontend**: Откройте URL frontend в браузере
   - Должна открыться страница логина

3. **API**: Попробуйте залогиниться с учетными данными админа

## 📝 Важные замечания

1. **Бесплатные планы имеют ограничения**:
   - Render: сервисы "засыпают" после 15 минут неактивности
   - Railway: ограниченное количество часов в месяц

2. **Безопасность**:
   - Обязательно измените пароль админа после первого входа
   - Используйте сильные SECRET_KEY и JWT_SECRET_KEY
   - Не коммитьте .env файлы в репозиторий

3. **Модели ML**:
   - Убедитесь, что файлы моделей (`classifier.h5`, `tokenizer.pkl` и т.д.) находятся в `backend/src/data/models_bin/`
   - Эти файлы должны быть в репозитории или загружены после деплоя

## 🐛 Решение проблем

### Backend не запускается
- Проверьте логи в Dashboard платформы
- Убедитесь, что все переменные окружения установлены
- Проверьте, что DATABASE_URL корректный

### Frontend не может подключиться к API
- Проверьте переменную `VITE_API_URL`
- Убедитесь, что CORS настроен правильно
- Проверьте, что backend доступен по указанному URL

### Ошибки базы данных
- Убедитесь, что таблицы созданы (выполните init_db.py)
- Проверьте DATABASE_URL
- Убедитесь, что БД доступна из backend сервиса

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Dashboard платформы
2. Убедитесь, что все шаги выполнены правильно
3. Проверьте документацию платформы

---

**Успешного деплоя! 🎉**

