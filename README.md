# 🎯 Система классификации товаров

Веб-приложение для автоматической классификации товаров по их названиям с использованием нейронных сетей.

## 📋 Описание

Система позволяет:
- Классифицировать товары по названиям в реальном времени
- Обрабатывать CSV файлы с большим количеством товаров
- Управлять пользователями (для администраторов)
- Просматривать топ-3 наиболее вероятные категории для каждого товара

## 🏗️ Архитектура

- **Backend**: Flask (Python) + PostgreSQL + Keras/TensorFlow
- **Frontend**: React 19 + Vite
- **ML Model**: Автокодировщик с классификатором (Dense layers)

## 🚀 Быстрый старт

### Локальная разработка

1. **Клонируйте репозиторий**
   ```bash
   git clone <your-repo-url>
   cd classification-system
   ```

2. **Настройте переменные окружения**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл
   ```

3. **Запустите через Docker Compose**
   ```bash
   docker-compose up --build
   ```

   Или вручную:
   
   **Backend:**
   ```bash
   cd backend
   pip install -r requirements.txt
   python src/main.py
   ```
   
   **Frontend:**
   ```bash
   cd frontend
   npm install
   npm run dev
   ```

4. **Инициализируйте базу данных**
   ```bash
   cd backend
   python src/init_db.py
   ```

5. **Откройте в браузере**
   - Frontend: http://localhost:5173
   - Backend API: http://localhost:5000

### Учетные данные по умолчанию

После инициализации БД:
- **Username**: `admin`
- **Password**: `admin123` (⚠️ измените после первого входа!)

## 📦 Деплой

Подробные инструкции по деплою на публичные платформы (Render.com, Railway.app) находятся в файле [DEPLOY.md](./DEPLOY.md).

### Краткая инструкция для Render.com:

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте PostgreSQL базу данных
3. Создайте Web Service для backend (Docker)
4. Создайте Web Service для frontend (Docker)
5. Настройте переменные окружения
6. Запустите `init_db.py` для инициализации БД

## 🧪 CI/CD

Проект настроен с GitHub Actions для автоматической проверки и сборки при пуше в репозиторий.

## 📁 Структура проекта

```
classification-system/
├── backend/              # Flask backend
│   ├── src/
│   │   ├── api/         # API endpoints
│   │   ├── database/    # Модели БД
│   │   ├── models/      # ML модели
│   │   ├── training/    # Скрипты обучения
│   │   └── data/        # Данные и модели
│   ├── Dockerfile
│   └── requirements.txt
├── frontend/            # React frontend
│   ├── src/
│   │   ├── pages/      # Страницы приложения
│   │   ├── services/   # API клиенты
│   │   └── styles/     # CSS стили
│   ├── Dockerfile
│   └── package.json
├── .github/workflows/   # CI/CD конфигурация
├── docker-compose.yml   # Локальная разработка
└── DEPLOY.md           # Инструкции по деплою
```

## 🔧 Технологии

### Backend
- Flask, Flask-CORS, Flask-SQLAlchemy
- PostgreSQL
- Keras/TensorFlow
- scikit-learn, pandas, numpy
- JWT аутентификация

### Frontend
- React 19
- React Router
- Axios
- Vite

## 📝 API Endpoints

- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/register` - Регистрация (admin only)
- `GET /api/users` - Список пользователей (admin)
- `DELETE /api/users/<id>` - Удаление пользователя (admin)
- `POST /api/predict_category` - Классификация одного товара
- `POST /api/predict_category_from_file` - Классификация из CSV
- `GET /health` - Health check

## 🔒 Безопасность

- JWT токены для аутентификации
- Хеширование паролей (Werkzeug)
- Роли пользователей (user/admin)
- CORS настройки
- Защита от SQL инъекций (SQLAlchemy ORM)

## 📄 Лицензия

Этот проект создан для учебных целей (ВКР).

## 👨‍💻 Автор

Студент университета

---

**Для подробной информации о деплое см. [DEPLOY.md](./DEPLOY.md)**

